import { ManageDevicesModule } from './manage-devices.module';

describe('ManageDevicesModule', () => {
    let ManageDevicesModule: ManageDevicesModule;

    beforeEach(() => {
        // ManageDevicesModule = new ManageDevicesModule();
    });

    it('should create an instance', () => {
        // expect(ManageDevicesModule).toBeTruthy();
    });
});
